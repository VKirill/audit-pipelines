# CI Hardening Audit Manifest — example
# Generated by `init.sh`. Edit before running phases if AI missed something.

version: "1.0"
generated_at: "2026-05-01T00:00:00+00:00"
generated_by: "claude-opus-4-7"
audit_dir: "results"

project:
  root: "/home/ubuntu/apps/myproject"
  name: "myproject"
  type: monorepo
  languages: [node, python]
  package_managers: [pnpm, pip]
  git_head: "abc1234"
  git_branch: "main"

github:
  owner: "VKirill"
  repo: "myproject"
  default_branch: "main"
  visibility: "public"
  ghas_enabled: false
  gh_cli_authenticated: true
  api_access: "full"

paths:
  workflows_dir: ".github/workflows"
  workflow_files:
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
  dependabot_config: ".github/dependabot.yml"
  renovate_config: ""
  codeowners: ""
  security_policy: ""
  pr_template: ""
  issue_templates: []

hints:
  unpinned_actions:
    - action: actions/checkout
      ref: v4
      file: .github/workflows/ci.yml
      line: 12
      current_sha: ""
      recommended_sha: "08c6903cd8c0fde910a37f88322edcfb5dd907a8"
    - action: actions/setup-node
      ref: v4
      file: .github/workflows/ci.yml
      line: 18

  excessive_permissions:
    - file: .github/workflows/ci.yml
      scope: workflow
      current: "(default — write-all)"
      recommended: "contents: read"

  persist_credentials_present:
    - file: .github/workflows/ci.yml
      line: 12
      job_name: build

  script_injection_risks:
    - file: .github/workflows/ci.yml
      line: 45
      expression: "${{ github.event.pull_request.title }}"
      severity: high

  dangerous_triggers: []

  long_lived_credentials:
    - secret_name: AWS_ACCESS_KEY_ID
      usage_files: [.github/workflows/deploy.yml]
      target_cloud: aws

  secrets_in_code: []

  branch_protection:
    enabled: false
    require_pr: false
    required_status_checks: []
    require_signed_commits: false
    require_linear_history: false
    required_approvals: 0
    dismiss_stale_reviews: false
    enforce_admins: false

  security_features:
    dependabot_alerts: true
    dependabot_security_updates: false
    secret_scanning: false
    secret_scanning_push_protection: false
    code_scanning: false
    private_vulnerability_reporting: false

  third_party_actions:
    - action: actions/checkout
      owner: actions
      usage_count: 3
      files: [.github/workflows/ci.yml, .github/workflows/release.yml]
      is_verified: true
    - action: codecov/codecov-action
      owner: codecov
      usage_count: 1
      files: [.github/workflows/ci.yml]
      is_verified: true

  missing_artefacts:
    - artefact: CODEOWNERS
      recommended_path: .github/CODEOWNERS
      severity: medium
    - artefact: SECURITY.md
      recommended_path: .github/SECURITY.md
      severity: high

  outdated_actions: []

mode:
  type: live
  gh_token_env: GH_TOKEN
  api_calls_count: 0

phase_plan:
  "00":
    detectors: []
    notes: "Setup — handled by init.sh"
  "01":
    detectors: [detect_workflows, detect_third_party_actions]
  "02":
    detectors: [detect_unpinned_actions, detect_outdated_actions]
  "03":
    detectors: [detect_excessive_permissions, detect_persist_credentials, detect_script_injection]
  "04":
    detectors: [detect_long_lived_credentials, detect_secrets_in_code]
  "05":
    detectors: [detect_branch_protection]
  "06":
    detectors: [detect_dependabot_config, detect_dangerous_triggers]
  "07":
    detectors: [detect_codeql_setup, detect_security_features]
  "08":
    detectors: [detect_codeowners, detect_security_md, audit_scorecard]
  "09":
    detectors: [synthesize_roadmap]
  "09a":
    detectors: [adversary_review]
  "10":
    detectors: [deep_dive]
