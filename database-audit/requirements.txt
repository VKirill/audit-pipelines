# database-audit pipeline — Python dependencies
#
# Install: pip install -r database-audit/requirements.txt
#
# Pinned to minimum versions; tested with these or later.

# YAML parsing for manifest.yml
PyYAML>=6.0,<7.0

# JSON Schema validation for manifest.schema.yml
# >=4.23 closes CVE-2023-22102 (DoS via malformed schema)
jsonschema>=4.23.0,<5.0

# === Optional but recommended ===

# (no extra Python deps — stdlib covers the rest)
